# A-Maze-ing Configuration File

# Maze dimensions (number of cells)
WIDTH=2
HEIGHT=2

# Entry and Exit coordinates (x,y format)
ENTRY=1,1
EXIT=0,0

# The destination file for the hexadecimal output
OUTPUT_FILE=\nmaze.txt

# Boolean flag to ensure the maze has exactly one solution path
PERFECT=False
